import { SanitizemePipe } from './sanitizeme.pipe';

describe('SanitizemePipe', () => {
  it('create an instance', () => {
    const pipe = new SanitizemePipe();
    expect(pipe).toBeTruthy();
  });
});
